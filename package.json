{
  "name": "cleanup-loader",
  "version": "0.0.1",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "watch": "npm run build --watch",
    "build": "babel src --out-dir dist"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-add-module-exports": "^0.2.1",
    "babel-preset-env": "^1.6.1"
  },
  "description": "Prevent webpack from emitting unwanted .js/.js.map files from unwanted files",
  "author": "Christian Tschugg",
  "repository": {
    "url": "git@github.com:iantsch/cleanup-loader.git",
    "type": "git"
  },
  "dependencies": {
    "loader-utils": "^1.1.0"
  }
}
